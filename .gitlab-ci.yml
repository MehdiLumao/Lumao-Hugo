image: registry.gitlab.com/pages/hugo/hugo_extended

pages:
  script:
    - npm ci
    - hugo --config config.toml --minify --baseURL="https://next.lumao.eu/"
  artifacts:
    paths:
      - public
  only:
    - master
